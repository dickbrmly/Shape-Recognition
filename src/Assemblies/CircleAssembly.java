package Assemblies;

import Position.Position;

//only need the radius because the origin will be (radius,radius)
public class CircleAssembly {
  Position origin = new Position();
  int radius = 0;
}
